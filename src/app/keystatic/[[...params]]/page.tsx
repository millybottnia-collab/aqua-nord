import { makePage } from "@keystatic/next/ui/app";
import keystaticConfig from "../../../../keystatic.config";

const KeystaticAdminPage = makePage(keystaticConfig);

export default KeystaticAdminPage;
